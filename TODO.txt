Checken ob meine edits funzen

-Conclusion schreiben
-git link (beni)

Was m.E. nicht getan werden muss:
-Gamma beta calculation ein zweites mal zeigen
-References auf den Code. Ich glaube wir können einfach den ganzen Code schicken und wenn jemand nachgucken will kann er/sie das gerne machen. (was wir schon gemacht haben ist natürlich schon und gut und können wir auch drinnen lassen)
-An einer Stelle steht 'add units', die sind doch schon da, oder?

Änderungen bei denen ich mir unsicher bin:
-Ich hab den Teil mit 'wir müssen pionen nicht rotieren, weil isotrop' weggelassen, weil ich vermute, da ist irgendwas falsch. Können wir ja nochmal besprechen, aber wenn wirs einfach weglassen sind wir safe.
-Zu determine optimal detector distance: die Anzahl der Berechnungen ist etwas unklar und doppelt angegeben, geht das noch einfacher?
-Bei der optimalen distance ist z wie ein Erwartungswert angegeben (<z>), das ist doch aber keine Erwartungswert, oder?
-Wie kommt die Fehlerrechung (Gleichung 14) zustande?


CONCLUSION2
As we have seen the optimal distance between the detector and the source is around 280 meters (for the precise values check the previous sections). Therefore the experiment can be conduced optimally without expanding the provided area.

For the beam with gaussian scattering around the z-axis, the optimal distance should be shorter than without scattering, because the deviation from the z-axis leads to a higher probability of the pions missing the detector. Therefore our results are in line with what's to be expected.

It’s also worth mentioning that if we were to account for the pion decay, the optimal distance would tend to be shorter, since the decays of pions can only reduce the acceptance at longer distances. Dave the man
